appId: ${APP_ID}
name: Wait for home tab after async navigation
tags:
  - skip
---
# Some flows navigate asynchronously after launch; wait for home before continuing.
- extendedWaitUntil:
    visible:
      id: "home-screen"
    timeout: 120000
