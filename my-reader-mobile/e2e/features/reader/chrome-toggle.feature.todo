Feature: 阅读器界面切换
  作为读者
  我希望点击阅读内容区域来切换阅读器界面状态
  以便在沉浸阅读和操作控制之间切换

  Background:
    Given 用户正在阅读 The Brothers Karamazov (book_id: 1)

  Rule: 基础状态间切换

    Scenario: 沉浸阅读时点击内容显示界面
      Given 用户正在沉浸阅读
      When 用户点击阅读内容区域
      Then 应显示阅读器界面控件

    Scenario: 界面可见时点击内容进入沉浸阅读
      Given 阅读器界面控件已显示
      When 用户点击阅读内容区域
      Then 应进入沉浸阅读状态

  Rule: 操作按钮展开状态下点击内容回到沉浸阅读

    Scenario: 界面展开时点击内容进入沉浸阅读
      Given 操作按钮已展开
      When 用户点击阅读内容区域
      Then 应进入沉浸阅读状态

  Rule: 展开状态下点击目录或设置进入对应面板

    Scenario: 操作按钮展开时点击目录打开目录面板
      Given 操作按钮已展开
      When 用户点击目录按钮
      Then 应打开目录面板

    Scenario: 操作按钮展开时点击设置打开设置面板
      Given 操作按钮已展开
      When 用户点击设置按钮
      Then 应打开设置面板

  Rule: 面板打开时点击内容关闭面板并显示界面

    Scenario: 目录面板打开时点击内容关闭面板
      Given 目录面板已打开
      When 用户点击阅读内容区域
      Then 应关闭目录面板
      And 应显示阅读器界面控件

    Scenario: 设置面板打开时点击内容关闭面板
      Given 设置面板已打开
      When 用户点击阅读内容区域
      Then 应关闭设置面板
      And 应显示阅读器界面控件